import os

class Config:
    SECRET_KEY = os.urandom(24)  # Random secret key for session
    SQLALCHEMY_DATABASE_URI = 'sqlite:///movie_reservation.db'  # SQLite for simplicity
    SQLALCHEMY_TRACK_MODIFICATIONS = False
